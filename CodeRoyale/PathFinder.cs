using System.Collections.Generic;
using System.Drawing;

namespace CodeRoyale
{
    // From https://redblobgames.github.io/circular-obstacle-pathfinding/
    internal sealed class Pathfinder
    {
        public Pathfinder(IEnumerable<Site> sites, int gridWidth = 1920, int gridHeight = 1000)
        {
            // Initialise graph
        }

        internal int GetShortestDistance(Point startPoint, Point endPoint)
        {
            // Reset paths
            
            // Create starting path
            
            // Get shortest estimated length
            
            // foreach possible paths from this point
            return 0;
        }
    }
}